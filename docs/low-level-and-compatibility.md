# Low-level API and compatibility

The lower-level `add`, `search`, CRUD, history, export, and import APIs remain available. The assistant-oriented `capture`, `context.build`, and chat namespaces also remain available.

See [Migrating from v3 to v4](./migration-v3-v4.md) for behavior and schema changes.
