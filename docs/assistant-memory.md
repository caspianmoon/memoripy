# Assistant memory

Use `capture(...)` for assistant interactions and `context.build(...)` or `recall(...)` to construct grounded context. Every automatic memory candidate passes through the v4 admission barrier before it can become durable state.

See [Admission and trust](./admission-and-trust.md) and the [API reference](./api-reference.md).
