# Service and storage

The base package includes in-memory and crash-safe file repositories. PostgreSQL support is optional through `memoripy[postgres]`.

The built-in HTTP service is intended for local development unless an API key, durable repository, transport security, tenancy, and deployment controls are configured by the host application.

See the [API reference](./api-reference.md) and [Security policy](../SECURITY.md).
